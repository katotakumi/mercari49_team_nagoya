-# - =begin
-# %h2 Log in
-# = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
-#   .field
-#     = f.label :email
-#     %br/
-#     = f.email_field :email, autofocus: true, autocomplete: "email"
-#   .field
-#     = f.label :password
-#     %br/
-#     = f.password_field :password, autocomplete: "current-password"
-#   - if devise_mapping.rememberable?
-#     .field
-#       = f.check_box :remember_me
-#       = f.label :remember_me
-#   .actions
-#     = f.submit "Log in"
-# = render "devise/shared/links"
-# - =end
.header
  .header__mercari
    = image_tag("users_logo.png", width: 200)
.main
  .login
    .login__panel
      .login__panel__noaccount
        .login__panel__noaccount__label アカウントをお持ちでない方はこちら
        .login__panel__noaccount__sign-up
          - if devise_mapping.registerable? && controller_name != 'registrations'
            = link_to "新規会員登録", new_registration_path(resource_name), class: "login__panel__noaccount__sign-up"
    = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      .login__form-inner
        .login__form-inner__form-box
          %br/
          = f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "メールアドレス"
        .login__form-inner__form-box
          %br/
          = f.password_field :password, autocomplete: "current-password", placeholder: "パスワード"
        .login__form-inner__button
          = f.submit "ログイン", class: "btn"
        .login__form-inner__password
          - if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'
            = link_to "パスワードをお忘れの方", new_password_path(resource_name)
.footer
  .footer__clearfix
    .footer__clearfix__option プライバシーポリシー
    .footer__clearfix__option メルカリ利用規約
    .footer__clearfix__option 特定商取引に関する表記
  .footer__logo
    = image_tag("footer_logo.png", width: 100)
  .footer__year © 2019 Mercari